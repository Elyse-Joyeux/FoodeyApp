---
description: 'A foodey-app app.'
labels: ['app', 'react']
---

A React application for foodey-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use foodey-app 
```

To run the app locally, run the following:

```
bit run foodey-app 
```
